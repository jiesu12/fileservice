server.port: 8100

eureka:
  client.serviceUrl.defaultZone: http://bt:8000/eureka
  instance:
    # used as search index name
    metadataMap.name: "test"
    preferIpAddress: true

fileservice:
  path.file: /home/jie/tmp/fileswim/file
  searchServiceName: searchservice

logging:
  level:
    root: INFO
    org.springframework: INFO
  file:
    max-size: 1MB
    max-history: 10